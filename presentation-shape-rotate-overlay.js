// Disabled in favor of unified PowerPoint-style presentation-hard-transform-controls.js
(() => {
  // Clear any existing legacy overlay if present
  document.getElementById('shapeRotateOverlay')?.remove();
  document.getElementById('shapeRotateButtons')?.remove();
})();