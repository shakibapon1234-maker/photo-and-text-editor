// presentation-shape-text.js - Clean inspector sync without blocking transform controls or double-clicks
(() => {
  // Content editing is intentionally double-click only on the slide itself.
  // Sidebar text fields have been removed so inspector controls stay style-only.
})();
